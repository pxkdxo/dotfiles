# ~/.config/git/config: git user global config
# see git(1)  git-config(1)

[advice]
  pushUpdateRejected

[color]
  branch = auto
  diff = auto
  status = auto
  interactive = auto

[color "decorate"]
  branch = green
  remoteBranch = red
  stash = blue
  tag = yellow

[core]

[credential]
  helper = cache --timeout 3600

[init]
  defaultBranch = master
  # templateDir = ~/.local/share/git/templates

[user]
  email = patrick.deyoreo@fox.com
  name = Patrick DeYoreo

[push]
  autoSetupRemote

[pull]
  rebase = false
