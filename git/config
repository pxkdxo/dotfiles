# ~/.config/git/config: git user global config
# see git(1)  git-config(1)

[advice]
	pushUpdateRejected

[color]
	branch = auto
	diff = auto
	status = auto
	interactive = auto

[color "decorate"]
	branch = green
	remoteBranch = red
	stash = blue
	tag = yellow

[core]
	hooksPath = ~/.local/share/git/hooks

[credential]
	helper = cache --timeout 43200

[init]
	templateDir = ~/.local/share/git/templates

[user]
	email = pdeyoreo@gmail.com
	name = Patrick DeYoreo
