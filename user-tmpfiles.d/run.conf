# run.conf : symlink to user runtime directory
# see tmpfiles.d(5), systemd-tmpfiles(8)

#Type   Path                              Mode    UID     GID   Age   Argument 

# Create a symlink to $XDG_RUNTIME_DIR at ~/.run
L     %h/.run                             -       -       -     -     %t
