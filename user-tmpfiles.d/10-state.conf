# user-tmpfiles.d/10-state.conf: the physical state tree at ~/.local/var
# see tmpfiles.d(5), user-tmpfiles.d/20-var.conf (path-ownership table)
#
# Literal paths, not %S/%L: those specifiers resolve through environment.d in
# the user manager and can point back at the paths they were meant to define.

# Type	Path			Mode	UID	GID	Age	Argument

# Create XDG_STATE_HOME (physical; environment.d points here)
d	%h/.local/var		0700	-	-	-

# Create the log directory with age-based cleanup of its contents
d	%h/.local/var/log	0700	-	-	~1w
