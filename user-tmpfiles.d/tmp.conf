# user-tmpfiles.d/tmp.conf
#
# Manage directory ~/.tmp to hold user tmpfiles
# see tmpfiles.d(5), systemd-tmpfiles(8)

#Type   Path                              Mode    UID     GID   Age   Argument 

# Setup ~/.tmp and remove old files
d       %h/.tmp                           01700   1000    1000  10d

# Exclude namespace mountpoints created with PrivateTmp=yes
x       %h/.tmp/systemd-private-%b-*       -       -      -     -
X       %h/.tmp/systemd-private-%b-*/tmp   -       -      -     -

# Remove top-level private temporary directories on each boot
R!      /tmp/systemd-private-*
